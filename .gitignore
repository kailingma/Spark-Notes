node_modules/
dist/
*.tsbuildinfo

# Server-side state: GitHub token, AI provider key, sync bookkeeping.
# Never commit this.
.spark/

# The default local space. Point SPARK_SPACE at your own notes directory,
# or remove this line if you want the space tracked in this repo.
space/

# Fonts are fetched with `npm run fonts` rather than committed.
apps/web/public/fonts/

.DS_Store
.env
.env.local
.env.bak

# Sensitive logs
nohup.out

# Agent working notes — local context, not part of the project.
AGENTS.md
CLAUDE.md
